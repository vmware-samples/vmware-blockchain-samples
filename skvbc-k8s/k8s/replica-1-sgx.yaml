apiVersion: v1
kind: Service
metadata:
  name: replica-1
  labels:
    tier: replica-1
spec:
  # comment or delete the following line if you want to use a LoadBalancer
  type: LoadBalancer
  # if your cluster supports it, uncomment the following to automatically create
  # an external load-balanced IP for the replica-1 service.
  # type: LoadBalancer
  ports:
  - port: 3712
    nodePort: 32012
    targetPort: 3712
  selector:
    tier: replica-1
---
apiVersion: apps/v1 #  for k8s versions before 1.9.0 use apps/v1beta2  and before 1.8.0 use extensions/v1beta1
kind: Deployment
metadata:
  name: replica-1
spec:
  selector:
    matchLabels:
      tier: replica-1
  replicas: 1
  template:
    metadata:
      labels:
        tier: replica-1
    spec:
      hostNetwork: true
      containers:
      - name: replica-1
        image: localhost:5000/skvbc-sgx-gramine:latest
        resources:
          requests:
            cpu: 1
            memory: 8Gi
            sgx.intel.com/enclave: 1
            sgx.intel.com/epc: 8Gi
          limits:
            sgx.intel.com/enclave: 1
            sgx.intel.com/epc: 8Gi
        env:
        - name: GET_HOSTS_FROM
          value: dns
          # If your cluster config does not include a dns service, then to
          # instead access environment variables to find service host
          # info, comment out the 'value: dns' line above, and uncomment the
          # line below:
          # value: env
        - name: replica_number
          value: "\x31"
        ports:
        - containerPort: 3712
          hostPort: 3712
