apiVersion: v1
kind: Secret
metadata:
  name: vmbc-secret-client-1-ethrpc
data:
  # cert private keys
  {{ range $path, $_ := .Files.Glob "client-1/config/ethrpc/*/*.pem" }}
  {{- $path | replace "/" "-" | indent 2 }}: {{ $.Files.Get $path | b64enc }}
  {{ end }}